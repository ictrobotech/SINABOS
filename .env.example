# Salin ke .env.admin.local pada komputer operator (jangan di-commit).
# Gunakan koneksi OWNER hanya pada skrip bootstrap/role, bukan runtime Cloudflare.
DATABASE_URL_ADMIN=
# DATABASE_URL runtime dan APP_SECRET dibuat oleh scripts/create-runtime-role.mjs.
